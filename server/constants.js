module.exports = {
    PORT: process.env.PORT || 3000,
    IP: process.env.IP || "127.0.0.1", // change to 0.0.0.0 for current ip

    LANGUAGES: {
        "javascript": {
            "extension": ".js"
        },
        "python": {
            "extension": ".py"
        }
    }
};